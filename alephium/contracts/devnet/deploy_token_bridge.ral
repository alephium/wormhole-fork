TxScript Main(
  deployer: DevnetDeployer,
  bytecode: ByteVec,
  governance: Governance,
  localChainId: U256,
  receivedSequence: U256,
  sendSequence: U256,
  wrappedAlphId: ByteVec,
  tokenBridgeFactory: TokenBridgeFactory,
  minimalConsistencyLevel: U256
) {
  deployer.deployTokenBridge{callerAddress!() -> 1 alph}(
    bytecode,
    governance,
    localChainId,
    receivedSequence,
    sendSequence,
    wrappedAlphId,
    tokenBridgeFactory,
    minimalConsistencyLevel
  )
}
