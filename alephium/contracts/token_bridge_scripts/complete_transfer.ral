import "../token_bridge/token_bridge_for_chain.ral"

TxScript Main(tokenBridgeForChainId: ByteVec, vaa: ByteVec) {
    let caller = callerAddress!()
    let tokenBridgeForChain = TokenBridgeForChain(tokenBridgeForChainId)
    tokenBridgeForChain.completeTransfer{caller -> dustAmount!()}(vaa, caller)
}
