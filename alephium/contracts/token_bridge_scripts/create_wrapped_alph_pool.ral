TxScript Main(payer: Address, tokenBridgeId: ByteVec, alphAmount: U256) {
    let tokenBridge = TokenBridge(tokenBridgeId)
    tokenBridge.createWrappedAlphPool{payer -> alphAmount}(payer, alphAmount)
}
