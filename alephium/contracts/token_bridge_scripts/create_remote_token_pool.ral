TxScript CreateRemoteTokenPool(
    payer: Address,
    attestTokenHandler: AttestTokenHandler,
    vaa: ByteVec,
    alphAmount: U256
) {
    attestTokenHandler.createRemoteTokenPool{payer -> alphAmount}(vaa, payer, alphAmount)
}
