import "token_bridge.ral"

TxScript Main(tokenBridgeId: ByteVec, vaa: ByteVec) {
    approveAlph!(uniqueTxInputAddress!(), 1000000000000) // dustAmount
    let tokenBridge = TokenBridge(tokenBridgeId)
    tokenBridge.completeUndoneTransfer(vaa)
}
