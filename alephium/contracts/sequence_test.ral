import "sequence.ral"

TxContract SequenceTest(
    mut next: U256,
    mut next1: U256,
    mut next2: U256,
    mut undone: ByteVec
) extends Sequence(next, next1, next2, undone) {
    pub fn check(seq: U256) -> () {
        checkSequence(seq)
    }
}
